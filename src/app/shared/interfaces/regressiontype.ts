export interface Regressiontype {
    ID: number;
    id: number; //for multiselect
    Name: string;
    name: string; //for multiselect
    code: string;
    description: string;
    isEditing?: boolean;
}
